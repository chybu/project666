Patient
check if min/max is really backend filter and implement backend
in patient/dashboard/reviewAppointments for both upcoming and past, i also want to have a search appointment like the search in receptionist/dashboard/appointments. upcoming filter is start/end date, doctor, type. Past filter is start/end date, doctor, type, and status
when clicking the past/upcoming, it will glow up or change color
in patient/dashboard/finances, i want search for Appointment Bills and Lab Bills like the search in receptionist/dashboard/appointments. Filter is start/end date, min/max amount, status
in patient/dashboard/pharmacy, i want it to display when is the next eligible, by what doctor
in patient/dashboard/pharmacy, i want search for prescriptions  like the search in receptionist/dashboard/appointments. Filter is start/end date, remaining refill, status, remaining refills, doctor
in patient/dashboard/records, lab technician column doesnt show for lab request. like it has the column for it but it doesnt load it
in patient/dashboard/records, i want to add a nurse column in Prechecks
in patient/dashboard/records, for prechecks, i want search for prechecks like the search in receptionist/dashboard/appointments. filter is min/max date, status, nurse
in patient/dashboard/records, for lab requests, i want search for lab requests like the search in receptionist/dashboard/appointments. filter is min/max date, status, doctor
in patient/dashboard/accessRequests, i want search for patient access request like the search in receptionist/dashboard/appointments. It will have 5 searches (all requests, pending, approved, rejected, revoked). Filter for all requests is doctor, record type, status, min/max date. for the rest of the searches, filter is doctor, record type, min/max date with a fixed status


Doctor
check if min/max is really backend filter and implement backend
doctor/dashboard/appointments, i want it to have Search Appointments like the receptionist/dashboard/appointments. But I want two search: one for upcoming, one for completed. the filter is start date, end date, type, patient
doctor/dashboard/prechecks, i want to have a search prechecks like the search in receptionist/dashboard/appointments. the filter is start date, end date, type, patient, status
doctor/prescriptions/create-form?appointmentId=xxx, i can only have one medicine in each prescription. I want a mechanism that i can add more prescription and delete the medicine
doctor/labs/create-form?appointmentId=, i can only have one lab test in each lab request. I want a mechanism that i can add more lab test and delete the lab test
doctor/dashboard/prechecks, i want search for prechecks like the search in receptionist/dashboard/appointments. filter is min/max date, status, nurse
doctor/dashboard/prescriptions, i want search for prescriptions  like the search in receptionist/dashboard/appointments. Filter is start/end date, remaining refill, status, remaining refills, patient
doctor/dashboard/labs, i want search for lab requests like the search in receptionist/dashboard/appointments. Filter is min/max date, patient, status
doctor/dashboard/shared-records, i want to show Prechecks, Lab Requests, Prescriptions as normal and also have corresponding search function
doctor/dashboard/access-requests, i want it to have Search for requests like the receptionist/dashboard/appointments. the filter is min/max date, patient, type, status

Receptionist
check if min/max is really backend filter and implement backend
receptionist/dashboard/create-appointment, i want the gui scale with the screen size